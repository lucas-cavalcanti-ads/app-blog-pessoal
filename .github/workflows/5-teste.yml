name: '[DEPLOY PRODUCTION]'

on:
  workflow_run:
    workflows: ["Build"]
    types: [requested]

jobs:
  teste:
    runs-on: ubuntu-latest
    steps:
      - name: update author information
        run: echo "TESTE"