name: '[RELEASE]'

on:
  pull_request:
    types:
      - closed
    branches:
      - developer

jobs:  
  teste:
    runs-on: ubuntu-latest
    steps:      
      - name: Create Pull Request
        run: |
          echo Criando Pull Request para Release
          echo "Pull Request Number - ${{ github.event.pull_request.head.sha }}
        #uses: peter-evans/create-pull-request@v4
        #with:            
        #    commit-message: Create Pull Request Release
        #    title: Create Pull Request Release
        #    body: |
        #      - Pull Request Release
        #      Auto-generated by [create-pull-request][1]              
        #    branch: release